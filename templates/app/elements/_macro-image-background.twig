{% macro render_background_image(img, theme) %}
    {% set backgroundImg = theme.link ~ '/public/img/img-bck-header-default.jpg' %}
    {% set image = img ? img : backgroundImg | towebp %}

    {{ 'style="background-image: url(' ~ get_image(image).src ~ ')"' }}
{% endmacro %}